---
description: Diff this repo's index.html against the blog-embedded copy and sync any changes
---

Compare `index.html` in this repo against `C:\Users\junpi\Desktop\watch-guide-blog\static\tools\watch-valuator\index.html`. These two files must stay byte-identical (see CLAUDE.md — this is a deliberate "keep both in sync by hand" setup, not something automated).

Show me the diff first. If they've diverged, ask which version is the source of truth before overwriting either one — don't assume.
